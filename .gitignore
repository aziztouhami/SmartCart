# IDEs & tools
.vscode/
.idea/
.claude/
*.swp
*.swo
*~
.DS_Store
desktop.ini

# Environment
.env
.env.local
.env.*.local

# Backend - Symfony
backend/vendor/
backend/var/
backend/.env.local
# Uploaded product/brand images are excluded on main (user-generated content,
# not source code) — this branch (with-product-uploads) deliberately tracks
# them instead, so the rule is commented out here.
# backend/public/uploads/
backend/database/seeders/assets/
backend/config/jwt/private.pem
backend/config/jwt/public.pem
backend/.phpunit.cache/
backend/.php-cs-fixer.cache

# Frontend - React
frontend/node_modules/
frontend/build/
frontend/dist/
frontend/.env.local
frontend/coverage/
frontend/.eslintcache

# Frontend - Cypress artifacts
frontend/cypress/videos/
frontend/cypress/screenshots/
frontend/cypress/downloads/

# npm / yarn logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker
.docker/
docker-compose.override.yml

# OS
Thumbs.db
*.log
*.bak

# Dependencies
node_modules/
vendor/

# Build outputs
dist/
build/

# Temporary files
*.tmp
temp/
.cache/
backend/.phpunit.result.cache

# Database
*.sql
!database/seeds/*.sql
!database/migrations/*.sql

# Internal/generated docs — not part of the shipped project
docs/
PROJECT_TRACKING.md
ARCHITECTURE.md

# Design source files at repo root — not used at runtime (the copy actually
# served by the app lives in frontend/public/assets/logo.png, which stays tracked)
/logo.png
/logo2.png