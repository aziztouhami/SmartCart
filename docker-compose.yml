services:
  # PostgreSQL Database
  postgres:
    image: postgres:15
    container_name: smartcart_postgres
    environment:
      POSTGRES_DB: ${DB_NAME:-scdb}
      POSTGRES_USER: ${DB_USER:-scu}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-scp}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=en_US.UTF-8"
      POSTGRES_HOST_AUTH_METHOD: md5
    ports:
      - "5436:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - smartcart_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-scu} -d ${DB_NAME:-scdb}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Mailpit — local dev SMTP server + web inbox (no real provider needed)
  mailer:
    image: axllent/mailpit
    container_name: smartcart_mailer
    ports:
      - "8025:8025"
      - "1025:1025"
    networks:
      - smartcart_network

  # Symfony Backend API
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: smartcart_backend
    environment:
      APP_ENV: ${APP_ENV:-dev}
      APP_DEBUG: ${APP_DEBUG:-1}
      DATABASE_URL: postgresql://${DB_USER:-scu}:${DB_PASSWORD:-scp}@postgres:5432/${DB_NAME:-scdb}
      APP_SECRET: ${APP_SECRET:-change_this_to_a_random_string_in_production}
      JWT_EXPIRATION: ${JWT_EXPIRATION:-3600}
      CORS_ALLOW_ORIGIN: ${CORS_ALLOW_ORIGIN:-http://localhost:3000}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-574596476297-no8lmjr18bf2jgsmnf7b6gv9nfsmuo44.apps.googleusercontent.com}
      MAILER_DSN: ${MAILER_DSN:-smtp://mailer:1025}
      ADMIN_EMAIL: ${ADMIN_EMAIL:-admin@smartcart.local}
      FRONTEND_URL: ${FRONTEND_URL:-http://localhost:3000}
      APP_NAME: ${APP_NAME:-SmartCart}
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
      GEMINI_MODEL: ${GEMINI_MODEL:-gemini-2.5-flash-lite}
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - /app/vendor
      - /app/var
    networks:
      - smartcart_network
    depends_on:
      postgres:
        condition: service_healthy
      mailer:
        condition: service_started
    command: >
      sh -c "composer install --no-interaction &&
             if [ ! -f config/jwt/private.pem ]; then
               mkdir -p config/jwt &&
               openssl genrsa -out config/jwt/private.pem 4096 &&
               openssl rsa -in config/jwt/private.pem -pubout -out config/jwt/public.pem;
             fi &&
             php bin/console doctrine:database:create --if-not-exists &&
             php bin/console doctrine:migrations:migrate --no-interaction &&
             PHP_CLI_SERVER_WORKERS=8 php -S 0.0.0.0:8000 -t public public/index.php"

  # React Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: smartcart_frontend
    environment:
      REACT_APP_API_URL: ${REACT_APP_API_URL:-http://localhost:8000/api}
    ports:
      - "3000:3000"
    networks:
      - smartcart_network
    depends_on:
      - backend

  # Nginx Reverse Proxy (optional, for production)
  nginx:
    image: nginx:alpine
    container_name: smartcart_nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./backend/public:/app/public
    networks:
      - smartcart_network
    depends_on:
      - backend
      - frontend
    profiles:
      - production

networks:
  smartcart_network:
    driver: bridge

volumes:
  postgres_data:
