App\DTO\Profile\ChangePasswordRequest:
    properties:
        currentPassword:
            - NotBlank: ~
        newPassword:
            - NotBlank: ~
            - Length: { min: 8, minMessage: 'New password must be at least 8 characters' }
